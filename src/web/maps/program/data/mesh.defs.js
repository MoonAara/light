> width of [element]: js { 
    return the.mesh.width(element); 
}
> height of [element]: js { 
    return the.mesh.height(element); 
}
> new [w]x[h] block: js { 
    return the.mesh.block(w,h); 
}
> {act|{ on,ivate},focus|{ on, in}} [line]: js { 
    return the.focus(element); 
} 

